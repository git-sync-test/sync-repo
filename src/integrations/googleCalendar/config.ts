import { IIntegrationConfig } from '@useparagon/core/integration';

/**
 * configuration for a googleCalendar
 */
const config: IIntegrationConfig = {
  description: 'Sync events with Google Calendar',
  overviewText:
    'Connect your Google account and sync events with your Google Calendar. Increase your productivity by ensuring your schedule is always up to date - without manual data entry.\n \nOur Google Calendar integration enables you to:\n \n• Automatically add new events to your Google Calendar\n• Sync events from your Google Calendar',
  showWatermark: true,
};

export default config;
